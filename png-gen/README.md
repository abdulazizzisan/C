# `png_gen.c` prints horizontal lines on the png image of specific color.
# `png_checkered.c` creates a checkered pattern png with specified image height width and bolock size in pixels.

### Run the program

```bash
gcc -Wall -o out/png_checkered png_checkered.c libs/TinyPngOut.c && out/png_checkered
```
